name: 🚀 Deploy Incremental (rsync only)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔐 Validate deploy secrets
        run: |
          set -e
          for k in SFTP_HOST SFTP_USERNAME SFTP_PASSWORD SFTP_REMOTE_DIR; do
            if [ -z "${{ secrets[k] }}" ]; then
              echo "❌ Missing secret: $k"
              exit 1
            fi
          done
          case "${{ secrets.SFTP_REMOTE_DIR }}" in
            "~"* )
              echo "❌ SFTP_REMOTE_DIR must be absolute (ex: /var/www/.../web)"
              exit 1
              ;;
          esac

      - name: 🧰 Install rsync + sshpass
        run: |
          sudo apt-get update
          sudo apt-get install -y rsync sshpass

      - name: 🚀 Deploy incremental (rsync)
        env:
          SSHPASS: ${{ secrets.SFTP_PASSWORD }}
        run: |
          set -e
          PORT="${{ secrets.SFTP_PORT != '' && secrets.SFTP_PORT || '22' }}"
          HOST="${{ secrets.SFTP_HOST }}"
          USER="${{ secrets.SFTP_USERNAME }}"
          REMOTE_DIR="${{ secrets.SFTP_REMOTE_DIR }}"

          echo "🚀 Deploying to ${USER}@${HOST}:${REMOTE_DIR}"

          sshpass -e rsync -az --human-readable --stats \
            -e "ssh -p ${PORT} -o StrictHostKeyChecking=no" \
            --exclude ".git" \
            --exclude ".env" \
            --exclude "node_modules/***" \
            --exclude "vendor/***" \
            --exclude "storage/***" \
            --exclude "bootstrap/cache/***" \
            "./" "${USER}@${HOST}:${REMOTE_DIR}/"
